apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mini-project-frontend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/sovichea99/mini-project-devops-gitops.git
    targetRevision: main
    path: frontend-helm
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: microservices
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true